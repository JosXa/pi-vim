// Test stub for @mariozechner/pi-tui
// Only the symbols actually imported by index.ts need to be present.

export function matchesKey(data, key) {
  if (key === "escape") return data === "\x1b";
  if (typeof key === "object" && key !== null && key.__type === "shiftAlt") return false;
  if (typeof key === "object" && key !== null && key.__type === "alt") return false;
  if (typeof key === "symbol") return false;
  return false;
}
export function truncateToWidth(s, _width, _fill) { return s; }
export function visibleWidth(s) { return s.length; }
export const Key = {
  shiftAlt: (_k) => Symbol(),
  alt: (_k) => Symbol(),
};
